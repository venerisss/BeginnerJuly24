package org.example.annotations;

public class Main {
    public static void main(String[] args) {
        student st = new student();
        st.printdetails();
        st.printdetails(); //the yellow highlight underline sows the code is depricated, but it will still still work if we run the code
    }
}
